#include <bits/stdc++.h>
using namespace std;

/*
    * SUBSEQUENCE: A contiguous or non-contiguous sequence, following the order.

*/

int main(){
    #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    #endif
    return 0;
}